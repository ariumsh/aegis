# Normalise to LF in the repository. The deploy target is Linux while
# development happens on Windows; pinning eol=lf keeps CRLF out of the history
# and stops diffs that are nothing but line endings.
* text=auto eol=lf

# Generated — collapse in diffs and exclude from language statistics.
pnpm-lock.yaml -diff linguist-generated
